[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gacdi"
dynamic = ["version"]
description = "Galaxy Cancer Data Importers — import NIH/NCI cancer datasets into Galaxy histories."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "GaCDI contributors" }]
keywords = ["galaxy", "cancer", "genomics", "gdc", "geo", "sra", "nih", "nci"]
dependencies = [
    "requests>=2.28",
    # Shared foundation: selection-bundle contracts, validators, and the retrying
    # HTTP session. In this monorepo install packages/gacdi_core editable first.
    "gacdi-core>=0.1,<0.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "requests-mock>=1.11",
]

[project.scripts]
gacdi = "gacdi.cli:main"
# Descriptive alias for the same entry point; the established `gacdi` CLI is retained.
gacdi-downloader = "gacdi.cli:main"

[project.urls]
Homepage = "https://github.com/paulocilasjr/GaCDI"

[tool.hatch.version]
path = "gacdi/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["gacdi"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "network: tests that perform real network downloads (deselect with -m 'not network').",
]
